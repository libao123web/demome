# Liaison 网络代理管理系统 - 交付文档

**交付日期：** 2026年1月8日  
**项目名称：** Liaison  
**版本：** v1.0.0  
**作者：** penglifang@penglifang.net

---

## 📖 项目概述

Liaison 是一款现代化的网络代理管理系统，用于管理内网应用的公网访问代理。系统提供了设备监控、连接器管理、应用配置和代理设置等核心功能，帮助企业轻松实现内网穿透和远程访问。

### 核心功能

- **代理管理**：创建和管理公网到内网应用的代理规则
- **设备监控**：监控内网设备状态、网卡信息和系统资源
- **连接器管理**：部署和管理边缘连接器，实现内网穿透
- **应用管理**：注册和管理内网应用（SSH、TCP、Web等）
- **用户认证**：基于 JWT 的用户登录和权限管理

---

## 🛠️ 技术栈

### 前端技术
- **框架**：React 18 + TypeScript 5.0
- **构建工具**：UmiJS 4.6 (Ant Design Pro Max)
- **UI 组件库**：Ant Design 5.4 + Pro Components 2.8
- **样式方案**：Tailwind CSS 3.4 + Less
- **包管理器**：pnpm 10.26

### 开发工具
- **代码格式化**：Prettier
- **类型检查**：TypeScript
- **Git Hooks**：lint-staged

---

## 📁 项目结构

```
network/
├── src/
│   ├── pages/              # 页面组件
│   │   ├── Proxy/          # 代理管理页面
│   │   ├── Device/         # 设备管理页面
│   │   ├── Connector/      # 连接器管理页面
│   │   ├── App/            # 应用管理页面
│   │   ├── Login/          # 登录页面
│   │   └── Settings/       # 设置页面
│   ├── services/           # API 服务层
│   │   ├── api.ts          # API 接口定义
│   │   └── index.ts        # 服务导出
│   ├── components/         # 公共组件
│   │   └── TableButtons/   # 表格按钮组件
│   ├── utils/              # 工具函数
│   │   ├── request.ts      # 请求封装
│   │   ├── format.ts       # 格式化工具
│   │   └── tableConfig.ts  # 表格配置
│   ├── hooks/              # 自定义 Hooks
│   ├── constants/          # 常量定义
│   ├── assets/             # 静态资源
│   ├── app.ts              # 应用配置
│   ├── access.ts           # 权限配置
│   └── typings.d.ts        # 类型定义
├── .umirc.ts               # UmiJS 配置
├── tailwind.config.js      # Tailwind 配置
├── package.json            # 依赖配置
└── tsconfig.json           # TS 配置
```

---

## 🌐 功能模块说明

### 1. 代理管理
- 创建公网代理，映射到内网应用
- 支持 TCP/UDP 协议
- 查看代理状态和关联应用
- 复制公网访问信息

### 2. 设备管理
- 查看设备列表（操作系统、CPU、内存、磁盘）
- 展示设备网卡信息（名称、IP、MAC）
- 按设备名或网卡 IP 搜索
- 查看设备详情

### 3. 连接器管理
- 创建连接器并获取安装命令
- 查看连接器在线状态和运行状态
- 扫描内网应用
- 关联设备，按设备名搜索

### 4. 应用管理
- 注册内网应用（SSH、Web、数据库等）
- 查看应用关联的代理
- 快速创建代理
- 按应用名或设备名搜索

### 5. 用户管理
- 用户登录/登出
- 修改密码
- JWT Token 自动续期和失效处理

---

## 🔌 后端 API 接口文档

### 基础信息
- **Base URL**: `http://49.232.250.11:8080`
- **API Prefix**: `/api/v1`
- **认证方式**: Bearer Token (JWT)
- **响应格式**: JSON

### 通用响应结构
```typescript
{
  code: number;       // 200: 成功, 401: 未授权, 500: 服务器错误
  message: string;    // 响应消息
  data?: any;         // 响应数据
}
```

---

### 1. 认证接口 (IAM)

#### 1.1 登录
```
POST /api/v1/iam/login
Content-Type: application/json

Request:
{
  "email": "string",
  "password": "string"
}

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "string",
    "user": {
      "id": number,
      "email": "string",
      "name": "string"
    }
  }
}
```

#### 1.2 获取当前用户信息
```
GET /api/v1/iam/profile
Authorization: Bearer <token>

Response:
{
  "code": 200,
  "data": {
    "id": number,
    "email": "string",
    "name": "string"
  }
}
```

#### 1.3 修改密码
```
PUT /api/v1/iam/user/password
Authorization: Bearer <token>

Request:
{
  "old_password": "string",
  "new_password": "string"
}
```

#### 1.4 登出
```
POST /api/v1/iam/logout
Authorization: Bearer <token>
```

---

### 2. 代理接口 (Proxies)

#### 2.1 获取代理列表
```
GET /api/v1/proxies?page=1&page_size=20&name=<search>
Authorization: Bearer <token>

Response:
{
  "code": 200,
  "data": {
    "proxies": [
      {
        "id": number,
        "name": "string",
        "description": "string",
        "port": number,              // 公网端口
        "application_id": number,
        "application_name": "string",
        "application_ip": "string",  // 内网应用 IP
        "application_port": number,  // 内网应用端口
        "device_name": "string",     // 所属设备名
        "status": number,            // 1: running, 2: stopped
        "created_at": "string",
        "updated_at": "string"
      }
    ],
    "total": number
  }
}
```

#### 2.2 创建代理
```
POST /api/v1/proxies
Authorization: Bearer <token>

Request:
{
  "name": "string",
  "description": "string",
  "port": number,
  "application_id": number
}

Response:
{
  "code": 200,
  "data": {
    "id": number,
    "name": "string",
    "port": number,
    ...
  }
}
```

#### 2.3 更新代理
```
PUT /api/v1/proxies/:id
Authorization: Bearer <token>

Request:
{
  "name": "string",
  "description": "string",
  "status": number
}
```

#### 2.4 删除代理
```
DELETE /api/v1/proxies/:id
Authorization: Bearer <token>
```

---

### 3. 设备接口 (Devices)

#### 3.1 获取设备列表
```
GET /api/v1/devices?page=1&page_size=20&name=<search>&ip=<ip_search>
Authorization: Bearer <token>

Query Parameters:
- name: 设备名搜索
- ip: 网卡 IP 搜索

Response:
{
  "code": 200,
  "data": {
    "devices": [
      {
        "id": number,
        "name": "string",
        "description": "string",
        "os": "string",              // linux/windows/macos
        "version": "string",
        "cpu": number,
        "memory": number,            // MB
        "disk": number,              // MB
        "interfaces": [              // 网卡信息
          {
            "name": "string",        // eth0, ens33
            "mac": "string",
            "ip": ["string"]         // IP 地址列表
          }
        ],
        "created_at": "string",
        "updated_at": "string"
      }
    ],
    "total": number
  }
}
```

#### 3.2 获取设备详情
```
GET /api/v1/devices/:id
Authorization: Bearer <token>

Response: 同设备对象结构
```

#### 3.3 更新设备
```
PUT /api/v1/devices/:id
Authorization: Bearer <token>

Request:
{
  "name": "string",
  "description": "string"
}
```

---

### 4. 连接器接口 (Edges)

#### 4.1 获取连接器列表
```
GET /api/v1/edges?page=1&page_size=20&name=<search>&device_name=<device_search>
Authorization: Bearer <token>

Query Parameters:
- name: 连接器名搜索
- device_name: 设备名搜索

Response:
{
  "code": 200,
  "data": {
    "edges": [
      {
        "id": number,
        "name": "string",
        "description": "string",
        "status": number,            // 1: running, 2: stopped
        "online": number,            // 0: offline, 1: online
        "device_id": number,
        "device_name": "string",     // 所属设备名
        "created_at": "string",
        "updated_at": "string"
      }
    ],
    "total": number
  }
}
```

#### 4.2 创建连接器
```
POST /api/v1/edges
Authorization: Bearer <token>

Request:
{
  "name": "string",
  "description": "string"
}

Response:
{
  "code": 200,
  "data": {
    "access_key": "string",
    "secret_key": "string",
    "command": "string"            // 安装命令（重要：字段名为 command）
  }
}
```

#### 4.3 更新连接器
```
PUT /api/v1/edges/:id
Authorization: Bearer <token>

Request:
{
  "name": "string",
  "description": "string",
  "status": number
}
```

#### 4.4 删除连接器
```
DELETE /api/v1/edges/:id
Authorization: Bearer <token>
```

#### 4.5 扫描应用
```
POST /api/v1/edges/:id/scan
Authorization: Bearer <token>

Response:
{
  "code": 200,
  "data": {
    "task_id": "string"
  }
}
```

#### 4.6 获取扫描任务状态
```
GET /api/v1/edges/:id/scan/:task_id
Authorization: Bearer <token>

Response:
{
  "code": 200,
  "data": {
    "task_id": "string",
    "task_status": "string",       // pending, running, completed, failed
    "applications": ["string"],
    "error": "string"
  }
}
```

---

### 5. 应用接口 (Applications)

#### 5.1 获取应用列表
```
GET /api/v1/applications?page=1&page_size=20&name=<search>&device_name=<device_search>
Authorization: Bearer <token>

Query Parameters:
- name: 应用名搜索
- device_name: 设备名搜索

Response:
{
  "code": 200,
  "data": {
    "applications": [
      {
        "id": number,
        "name": "string",
        "application_type": "string",  // ssh, tcp, web, etc.
        "ip": "string",
        "port": number,
        "edge_id": number,
        "device": {
          "id": number,
          "name": "string"
        },
        "proxy_name": "string",        // 关联的代理名称
        "proxy_port": number,          // 关联的代理端口
        "created_at": "string",
        "updated_at": "string"
      }
    ],
    "total": number
  }
}
```

#### 5.2 创建应用
```
POST /api/v1/applications
Authorization: Bearer <token>

Request:
{
  "name": "string",
  "application_type": "string",
  "ip": "string",
  "port": number,
  "edge_id": number,
  "device_id": number
}
```

#### 5.3 更新应用
```
PUT /api/v1/applications/:id
Authorization: Bearer <token>

Request:
{
  "name": "string"
}
```

#### 5.4 删除应用
```
DELETE /api/v1/applications/:id
Authorization: Bearer <token>
```

---

## 🚀 部署说明

### 开发环境部署

#### 1. 环境要求
- Node.js >= 16.x
- pnpm >= 8.x
- 现代浏览器（Chrome/Firefox/Edge 最新版）

#### 2. 安装依赖
```bash
cd /d/work/demome/network
pnpm install
```

#### 3. 配置后端地址
编辑 `.umirc.ts`，修改 proxy 配置：
```typescript
proxy: {
  '/api': {
    target: 'http://your-backend-server:8080',
    changeOrigin: true,
  },
}
```

#### 4. 启动开发服务器
```bash
pnpm dev
# 或
pnpm start
```

访问：http://localhost:8000

---

### 生产环境部署

#### 1. 构建生产版本
```bash
pnpm build
```

构建产物位于 `dist/` 目录。

#### 2. 部署到 Nginx

**Nginx 配置示例：**
```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    # 前端静态文件
    location / {
        root /var/www/liaison/dist;
        try_files $uri $uri/ /index.html;
        index index.html;
    }
    
    # 代理后端 API
    location /api/ {
        proxy_pass http://49.232.250.11:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # Gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
}
```

#### 3. 部署步骤
```bash
# 1. 上传构建产物
scp -r dist/* user@server:/var/www/liaison/dist/

# 2. 重启 Nginx
sudo nginx -t
sudo systemctl reload nginx
```

#### 4. Docker 部署（可选）

**Dockerfile 示例：**
```dockerfile
FROM nginx:alpine

# 复制构建产物
COPY dist/ /usr/share/nginx/html/

# 复制 Nginx 配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

**构建和运行：**
```bash
docker build -t liaison-frontend .
docker run -d -p 80:80 liaison-frontend
```

---

## ⚙️ 环境变量配置

前端应用通过 `.umirc.ts` 配置环境变量：

```typescript
export default defineConfig({
  define: {
    API_BASE_URL: process.env.API_BASE_URL || 'http://49.232.250.11:8080',
  },
  proxy: {
    '/api': {
      target: process.env.API_BASE_URL || 'http://49.232.250.11:8080',
      changeOrigin: true,
    },
  },
});
```

**构建时指定环境变量：**
```bash
API_BASE_URL=https://api.production.com pnpm build
```

---

## 🔒 安全说明

### 认证机制
- 使用 JWT Token 进行用户认证
- Token 存储在 localStorage
- Token 失效（401）自动跳转登录页
- 请求拦截器自动添加 Authorization 头

### 权限控制
- 未登录用户自动跳转到登录页
- 支持基于路由的权限控制
- 敏感操作需要二次确认

### 安全建议
- 生产环境建议使用 HTTPS
- 定期更换 JWT Secret
- 设置合理的 Token 过期时间
- 启用 CORS 白名单
- 配置 CSP 策略

---

## 📊 监控和运维

### 性能监控
- 使用浏览器 DevTools 监控性能
- 关注首屏加载时间和资源大小
- 定期检查控制台错误

### 日志记录
- 前端错误自动上报（可配置）
- 查看 Network 面板排查 API 问题
- 使用 Redux DevTools 调试状态

### 常见问题排查

#### 1. 登录失败
- 检查后端服务是否正常
- 验证用户名密码是否正确
- 查看浏览器控制台错误

#### 2. API 请求失败
- 检查网络连接
- 验证 Token 是否有效
- 查看后端日志

#### 3. 页面加载缓慢
- 清除浏览器缓存
- 检查网络带宽
- 使用 CDN 加速静态资源

---

## 📞 技术支持

**项目作者：** penglifang@penglifang.net  
**文档版本：** v1.0.0  
**最后更新：** 2026年1月8日

---

## 📄 附录

### A. 浏览器兼容性
- Chrome >= 90
- Firefox >= 88
- Edge >= 90
- Safari >= 14

### B. 第三方依赖
- 详见 `package.json`
- 定期更新依赖以修复安全漏洞
- 使用 `pnpm audit` 检查安全问题

### C. 版本历史
- **v1.0.0** (2026-01-08): 初始版本发布

---

*本文档包含了 Liaison 系统的完整部署和 API 说明，如有疑问请联系技术支持。*
